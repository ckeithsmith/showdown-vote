{
  "name": "showdown-vote",
  "version": "0.1.0",
  "private": true,
  "main": "server/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently -k \"nodemon server/index.js\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "test": "node --test",
    "heroku-postbuild": "npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.9"
  }
}
